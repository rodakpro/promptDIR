// Dead file — superseded by the URL-driven library list (app/library/page.tsx) plus
// components/library/library-search.tsx and library-asset-card.tsx.
// Emptied because the sandbox cannot unlink files; safe to delete from the repo.
export {};
